To configure this module:

- Go to *Sales > Configuration > Settings*.
- In the *Quotations & Orders* section, enable **Disable Cancel Warning**
   to cancel sales orders directly without displaying the cancellation
   confirmation wizard.
- Disable the option to keep the standard cancellation confirmation wizard.
